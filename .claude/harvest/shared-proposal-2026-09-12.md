# Shared-memory proposals — telegrind, 2026-09-12

Harvested from 6 session digests against `35574a2`. Nothing here has been
written; `/memory-review` applies what is approved. Format:

`tier | action | the fact, as exact text to paste | target file | source | confidence`

## global

global | add | **A hand-written test fake that supplies the wrong type hides the exact bug it was written to cover.** telegrind's suite stayed green through a `DataError` on every message edit because its `SimpleNamespace` fake passed a `datetime` where Telegram sends an `int`. When a fake stands in for an external API, it must carry the types that API actually sends, not the types the code wishes for. | ~/.claude/memory/global.md | e03cc61f-c9c4-4727-a842-384c32ff5a0e | high
global | add | **Postgres does not evaluate an aggregate's argument expression for rows its `FILTER` clause excludes.** Measured 2026-09-11 on a live table holding `{"a":5}`, `{"a":"около 500"}` and `{"b":1}`: `sum(cast(f->>'a' as float)) filter (where jsonb_typeof(f['a']) = 'number')` returned 5 and did not raise. That is what makes a type-guarded aggregate over heterogeneous JSONB safe — a bare cast fails the whole query, not one row. | ~/.claude/memory/global.md | e03cc61f-c9c4-4727-a842-384c32ff5a0e | high
global | add | **In SQLAlchemy 2 a bare read autobegins a transaction, so `session.begin()` is not re-entrant.** A read taken before an explicit `async with session.begin():` makes the `begin()` raise `InvalidRequestError: A transaction is already begun on this Session`. Verified against a live `Session`. Any read done "outside a transaction" must itself be wrapped in one. Hand-written fake sessions that yield from `begin()` unconditionally cannot catch this. | ~/.claude/memory/global.md | e03cc61f, 76002530 | high
global | add | **`dateparser`'s `RELATIVE_BASE` must be the user's local wall clock, not UTC.** At 02:00 in a UTC+6 zone it is still the previous day in UTC, so a naive UTC base makes «сегодня» resolve one day behind what the writer meant. | ~/.claude/memory/global.md | e03cc61f-c9c4-4727-a842-384c32ff5a0e | high
global | add | **`dateparser.parse` returns `None` — no date at all, not a wrong one — for every Russian `<day> <time-of-day>` compound**: «вчера вечером», «вчера утром», «позавчера вечером», «в понедельник утром», «вчера днём», «вчера ночью», «сегодня вечером». Measured against dateparser 1.2 on 2026-09-11. The silent `None` means the phrase falls through to whatever fallback the caller supplies, so a relative date quietly becomes "now". Strip the time-of-day qualifier with a regex and re-parse the day alone. | ~/.claude/memory/global.md | e03cc61f-c9c4-4727-a842-384c32ff5a0e | high
global | add | **A relative date parser is the wrong tool for a period boundary.** «август» through `dateparser` yields *this day* in August, not the month. Month ranges have to arrive as explicit ISO dates, half-open with the upper bound exclusive so nothing lands in two months. | ~/.claude/memory/global.md | e03cc61f-c9c4-4727-a842-384c32ff5a0e | high
global | add | **Python's `float()` accepts an underscore as a digit separator** — `float("4821_1")` is `48211.0` — so it is a useless oracle for "this string is not a number to a spreadsheet". Google Sheets does not accept it. A test asserting non-numeric-ness needs a regex or a Sheets-shaped parser. | ~/.claude/memory/global.md | 72bed015-9d07-426c-adcf-8d75ace91f79 | high
global | add | **`docker compose config --format json` prints every resolved environment variable, live API keys and bot tokens included, straight into the transcript.** Never dump a whole service's config to inspect it — query the single key you need, or pipe through a redacting `sed`. | ~/.claude/memory/global.md | e03cc61f-c9c4-4727-a842-384c32ff5a0e | high
global | add | **An illegal HTTP header makes the Anthropic client print the whole API key in its failure output.** A `.env` with CRLF endings sourced in a shell carries the trailing `\r` into the value and triggers exactly this; that is how a live key reached a session transcript on 2026-09-11. Load `.env` in-process (python-dotenv strips the `\r`) or `tr -d '\r'`, and redact `sk-ant-[A-Za-z0-9_-]+` from anything that could surface one. | ~/.claude/memory/global.md | e03cc61f-c9c4-4727-a842-384c32ff5a0e | high
global | add | **Anthropic structured outputs are `client.messages.create(..., output_config={"format": {"type": "json_schema", "schema": <dict>}})`, then `json.loads(resp.content[0].text)`** — not `messages.parse`, which needs a static Python type and so cannot serve a schema built at runtime. Measured on `claude-haiku-4-5` with `anthropic` 0.97.0 on 2026-09-09: `anyOf` nested inside an array's `items` with a `const` discriminator works; Cyrillic property names work verbatim; `{"type": "string", "format": "date-time"}` is honoured (offset included); `additionalProperties: false` on every branch is accepted; and a PARTIAL `required` list is accepted, so an optional field arrives as a MISSING KEY rather than an empty string — coercion has to handle absence. | ~/.claude/memory/global.md | 72bed015-9d07-426c-adcf-8d75ace91f79 | high
global | add | **`cache_control` silently no-ops below a model's minimum cacheable prefix** — measured `cache_creation_input_tokens == 0` at 1047 input tokens on `claude-haiku-4-5`. Keeping the breakpoint costs nothing and starts working as the prefix grows, but no test may assert a cache hit. Corollary for prompt design: caching is a prefix match, so anything per-message or per-chat — the current timestamp above all — belongs in the user message, never interpolated into the cached system prefix. | ~/.claude/memory/global.md | 72bed015-9d07-426c-adcf-8d75ace91f79 | high
global | add | **`claude-haiku-4-5` is the `budget_tokens` generation and does not take adaptive thinking**, so the `thinking` parameter is omitted entirely for it. It is the one current model where the modern default is the wrong one. | ~/.claude/memory/global.md | 72bed015-9d07-426c-adcf-8d75ace91f79 | medium
global | add | **Headless `claude -p` session control.** `--session-id <uuid>` starts a session under an id the caller chooses; `--resume <uuid>` continues it; `--resume <base> --fork-session --session-id <new>` forks the history under a new caller-chosen id. `--output-format json` returns `result`, `is_error` and `session_id`, and the transcript is one `.jsonl` per session under `~/.claude/projects/<cwd-slug>/`. Because the id is the caller's to pick, it can be derived rather than stored. Probed on g15, 2026-09-11/12. | ~/.claude/memory/global.md | c4185692-a9db-4208-a6d5-6d0bd33d1593 | high
global | add | **A cold `claude -p` session pays the whole user-scope preamble — CLAUDE.md, hooks, skills, MCP servers — as cache-creation tokens.** Measured ~20,000 cache-creation tokens and $0.21 on g15 (2026-09-11/12) against ~250 tokens on a resume, roughly eighty times the input; a fork off an already-warm base paid 2.9k creation against 32k cache read. Any design that spawns a fresh headless session per message pays for the preamble every time; a warm base forked per turn removes that. | ~/.claude/memory/global.md | c4185692-a9db-4208-a6d5-6d0bd33d1593 | high
global | add | **An unattended `claude -p` run needs BOTH `--permission-mode bypassPermissions` and `--permission-prompts none`.** Without the second, anything that would have prompted is silently DENIED rather than allowed, so the run appears to work and quietly does nothing. | ~/.claude/memory/global.md | c4185692-a9db-4208-a6d5-6d0bd33d1593 | high
global | add | **A session id derived from the message being replied to does not survive three turns without `--fork-session`.** Trace: Claude answers M1 in `uuid5(M1)` and replies to M1; a user reply F1 to that answer resolves back to M1, and resume-in-place continues `uuid5(M1)`, whose answer replies to F1; the next user reply resolves to F1, and `uuid5(F1)` was never created. Forking each turn into its own derived id is what makes the scheme self-consistent, and it is one extra flag. | ~/.claude/memory/global.md | 76002530-146a-4ddc-8a01-5597be6c778a | high
global | add | **The Gortex PreToolUse hook in deny posture rejects the ENTIRE Bash command when any part of it reads or greps indexed source**, so a heredoc that mutates a file earlier in the same `&&` chain silently never runs. This bit three times in one session, once skipping a migration-docstring edit unnoticed. A bare grep word matching an indexed symbol name is enough to trigger it; a regex metacharacter (`\b`) or a quoted pattern forces a text search and is allowed. Never mix reads of indexed source into a chain that also mutates. | ~/.claude/memory/global.md | e03cc61f-c9c4-4727-a842-384c32ff5a0e | high
global | add | **Gortex's Python resolver does not unwrap an async method's return type on its own, but it honours an explicit local annotation.** Changing `conf = await self.cfg.get_data()` to `conf: Config = await ...` upgraded the call edge from `text_matched` to `ast_resolved`; applied at three call sites in one small repo it moved high-confidence calls from 53.8% to 58.5% and weak edges from 6 to 3. Measured 2026-09-08. | ~/.claude/memory/global.md | 808ef927-e3ee-4288-a8d2-1e268fadadf8 | high
global | add | **Annotating an instance attribute set in a base class `__init__` and used in a subclass method does not improve Gortex edge resolution** — neither `self.cfg: ConfigSheet = ...` nor a class-body `cfg: ConfigSheet` moved any weak edge. That is a resolver limit across the inheritance boundary, not an annotation gap; do not spend effort there. Measured 2026-09-08. | ~/.claude/memory/global.md | 808ef927-e3ee-4288-a8d2-1e268fadadf8 | high
global | add | **A repo's Gortex resolution ceiling is its third-party dependencies.** In a small app repo the remaining unknown call edges are almost all calls into libraries, which `analyze external_calls` shows indexed at 0 symbols. No amount of first-party annotation touches those, so a repo tops out well short of 100% high-confidence calls unless the virtualenv itself is indexed. Know the ceiling before chasing the last percent. | ~/.claude/memory/global.md | 808ef927-e3ee-4288-a8d2-1e268fadadf8 | high
global | add | **Gortex does model decorator-registered aiogram handlers.** The claim that they make it report "0 usages" was measured and found wrong: `analyze dead_code` returned 0 and `annotation_users` showed `router.message` with 6 users. Return-type annotations on handlers are not worth adding for the graph's sake — handlers are called by the framework and add no edges either way. Measured 2026-09-08. | ~/.claude/memory/global.md | 808ef927-e3ee-4288-a8d2-1e268fadadf8 | medium
global | add | **Claude Code hooks merge additively across settings scopes**, so a project-scope `settings.json` can never remove a user-scope hook — there is no way to silence a hook in one repo only. And deleting one from `~/.claude/settings.json` alone is not enough on a fleet-provisioned box: bootstrap's `gortex_merge_hooks` copies hooks out of `~/.claude/settings.local.json` into `settings.json`, so the entry returns at the next bootstrap unless it is removed from both (or gortex is reinstalled with `--no-hooks`). | ~/.claude/memory/global.md | 808ef927-e3ee-4288-a8d2-1e268fadadf8 | high
global | add | **`gortex init --agents claude-code` writes a project-scope `.claude/settings.json` granting `mcp__gortex__*` auto-approval, which everyone who checks the repo out inherits.** In a repo where a push deploys, that deserves a deliberate decision rather than an accepted default. | ~/.claude/memory/global.md | 808ef927-e3ee-4288-a8d2-1e268fadadf8 | medium
global | add | **`pkill -f "<pattern>"` run from an agent Bash tool matches the tool's own wrapper process and kills the shell (exit 144)**, so the restart that was meant to follow never runs and the service stays down looking like it crashed. Use a pattern that cannot self-match, and start long-running processes detached — `setsid nohup … >> log 2>&1 < /dev/null & disown` — so a session ending or a background-task reaper does not take them with it. | ~/.claude/memory/global.md | 72bed015-9d07-426c-adcf-8d75ace91f79 | high
global | add | **A Claude Code background task can be killed with "stopped because the system is running low on memory" while memory is in fact fine** — measured on g513ie with 14.9 GB of 31 GB available when the process received SIGTERM. Treat that notification as a harness reap rather than evidence of a memory problem, and detach anything that must survive it. | ~/.claude/memory/global.md | 72bed015-9d07-426c-adcf-8d75ace91f79 | medium
global | add | **A Google service account has no Drive of its own.** `about.get` returns `storageQuota {limit: "0", usage: "0"}`, and both `files.create` of a Google-native spreadsheet and `files.copy` fail 403 `storageQuotaExceeded`; creating inside a shared folder does not dodge it, because quota follows the creator. So a service-account bot can read and write any workbook shared with it forever but can never create or own one. The only way for a bot to create a spreadsheet is OAuth as a real user (the `drive.file` scope is non-sensitive, needs no verification, and its refresh token does not expire — but it can only touch files the app itself created, so importing a pre-existing workbook must happen before switching). A 403 `SERVICE_DISABLED` is a different error: the Drive API is not enabled in the GCP project at all. | ~/.claude/memory/global.md | 72bed015-9d07-426c-adcf-8d75ace91f79 | high
global | add | **Google Sheets `values.append` places rows after the sheet's *data extent*, not after the last row of the range you declare.** On a worksheet carrying user formula columns filled down every row that extent sits far below the range just cleared: measured on a real 3501-row workbook, a rebuild appended all 3501 rows starting at row 3505, leaving 3503 blank rows above and growing the grid from 3502 to 7005. Write at an explicit `A<row>` range instead, growing the grid when needed and never shrinking it. Separately, a worksheet created with `rows=1` makes any `A2:E` range a hard 400 (`exceeds grid limits. Max rows: 1`), not a no-op — create with a real row allowance and guard clear-style operations with a `row_count <= 1` early return. | ~/.claude/memory/global.md | 72bed015-9d07-426c-adcf-8d75ace91f79 | high

## host:g15

host:g15 | add | **Roughly 4 GB of VRAM, which caps a local model at about 4B parameters in a 4-bit quant**; 7–8B spills to CPU and gets slow. The figure is recalled rather than measured — `nvidia-smi` was failing with a driver/library version mismatch that needs a reboot to clear. For a deferred-extraction workload the number that matters is how long one 200-message window takes, not tokens per second. | ~/.claude/host-memory.md | e03cc61f-c9c4-4727-a842-384c32ff5a0e | medium

## host:latitude

host:latitude | add | **Claude Code is not installed on latitude.** Node 20 is present, and the subscription login is interactive and has to be done by hand once. The agent wrappers arrive on their own because latitude carries the `agents` role and `bootstrap.sh` deploys the plugin and skills there. Relevant because telegrind production runs on this box and a meta layer that spawns `claude -p` would need it. | ~/.claude/host-memory.md (latitude's own branch) | c4185692-a9db-4208-a6d5-6d0bd33d1593 | medium

## Notes for the reviewer

- Sessions `72bed015` (2026-09-08→10) and `808ef927`/`53ef4f88` largely describe
  the **pre-dialogue-first** telegrind: the Google Sheets workbook, the
  `_categories` registry, `/link` `/import` `/rebuild`, `origin="imported"`
  facts, `delete_record`, `extract_expense`. That whole layer was deleted on
  2026-09-11 (`248fe9d`). Repo-specific rows from those sessions were dropped as
  obsolete; only the vendor-API and tooling facts above survive, because they are
  true independently of telegrind.
- Sessions `c4185692` and `76002530` design the Claude meta layer. Almost every
  repo-specific decision from them is already written into
  `docs/superpowers/specs/2026-09-11-claude-meta-layer-design.md` and the
  `2026-09-12` plan, so they were dropped as already-recorded rather than
  harvested. What is proposed above from them is only the machine-level
  `claude -p` behaviour, which belongs outside this repo.
- **Uncovered Track B drift for the next run.** Two commits landed on
  `freeform-facts` *after* this harvest's doc pass and are not covered by it:
  `9d69d79` ("the verdict column, and the tail reads it instead of
  extractable") and `3759228` ("give non-/q commands VERDICT_SYSTEM, and
  preserve verdict across an edit"). Together they add `message.verdict` — four
  values (`fact`, `question`, `talk`, `system`), never null, `system` for every
  slash command that is not `/q` and for everything the bot itself sends — make
  the extraction tail select on `verdict`, and leave `extractable` written but
  no longer read. The root `CLAUDE.md` Architecture section does not mention
  `verdict` anywhere. That is real, uncaught drift; pick it up next time
  regardless of how the baseline commit is computed. `last_refresh.commit` in
  `kb-harvest-state.json` is `35574a2` because that is the tree this pass
  actually read, not the branch tip at commit time.
